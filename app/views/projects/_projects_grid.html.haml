- projects.each do |p|
  .project.c4
    %h3= p.name
    %p= p.description
    %p= p.address
    %p= p.team_mode ? "Team" : "Individual"
    %p= p.related_fields_of_study.join(", ")
    %p= p.related_student_passions.join(", ")